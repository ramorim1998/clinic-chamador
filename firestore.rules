rules_version = '2';
service cloud.firestore {
    match /databases/{database}/documents {
        match /calls/{callId} {
            allow read: if true; // chamadores precisam ler
            allow create: if request.auth != null; // só recepção autenticada cria
            allow delete, update: if false;
        }
    }
}